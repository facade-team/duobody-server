import express from 'express'
import Authenticate from '../middlewares/Authenticate'
// import { getDate } from '../../../services/trainerService'

const router = express.Router()

export default router
